{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pip install --upgrade pip setuptools wheel && pip install -r requirements-railway.txt"
  },
  "deploy": {
    "startCommand": "python -m flask db upgrade && python init_db.py && gunicorn 'app:create_app(\"production\")' --bind 0.0.0.0:$PORT",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
